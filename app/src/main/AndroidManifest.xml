<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.books">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.LibraryBookClickedActivity"></activity>
        <activity android:name=".Activities.UsersSetUpActivity" />
        <activity android:name=".Activities.EmailAndPasswordActivity" /> <!-- <activity  android:name=".fragments.HomeFragment" /> -->
        <activity android:name=".Activities.ReadBookActivity" />
        <activity android:name=".Activities.ClickBookCoverActivity" />
        <activity android:name=".Activities.ScienceActivity" />
        <activity android:name=".Activities.RomanceActivity" />
        <activity android:name=".Activities.MedicalsActivity" />
        <activity android:name=".Activities.MagicalActivity" />
        <activity android:name=".Activities.HistoryActivity" />
        <activity android:name=".Activities.FictionActivity" />
        <activity android:name=".Activities.FantasyActivity" />
        <activity android:name=".Activities.ComicActivity" />
        <activity android:name=".Activities.ClassicActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>